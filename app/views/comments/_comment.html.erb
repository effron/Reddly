<p><%= link_to comment.user.username, comment.user %>: <%= comment.body %></p>
<%= render partial: "comments/new_comment", locals: {comment: comment} %>
<ul>
<% comments_by_parent[comment.id].each do |child| %>
<li>
  <%= render partial: "comments/comment",
              locals: { comment: child,
                        comments_by_parent: comments_by_parent } %>
</li>
<% end %>
</ul>